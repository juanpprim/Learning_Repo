# Godot 4+ import cache — regenerated by the editor / `--import` on first run.
# The per-asset *.import sidecar files ARE committed (they carry stable UIDs
# and import settings); only this machine-specific compiled cache is ignored.
.godot/

# Exported builds
/build/
*.exe
*.pck
*.zip
